# Lectrax Admin

Platform-admin deployment for Lectrax. Generated from the main app via `npm run export:admin`.

See [../README.md](../README.md) for setup, environment variables, and push instructions.

```bash
npm install
cp .env.example .env.local
npm run dev
```
